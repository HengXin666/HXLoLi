# 待答问题 (答题卡)

## 0x00 26f3a9c1 begin {
**Q**:
对比对象确认: 你说的 "cloud code" 是指哪个?

- **Claude Code (Anthropic)** — 你本机装了 @anthropic-ai/claude-code 2.1.219, 这是 "Claude Code" 被语音转写成 "cloud code" 最常见的读法.
- **Codex CLI (OpenAI)** — 你本机装了 @openai/codex 0.147.0, 且你之前的笔记 (RTK项目使用说明与Codex本地心得) 大量围绕 Codex.

**推荐答案 (Recommended)**: **Claude Code**. 理由: "cloud code" 与 "Claude Code" 发音完全一致, 而 Codex 你通常会直接说 "Codex". 两条路我都会做: 选 Claude Code 后, 本文对比章节以 Claude Code 为主, 但我会在结尾补一节 Codex 的关键差异 (因为它俩的上下文压缩策略完全不同, 对比价值高).

注意: 无论选哪个, DSH 一侧的源码引用都以本机 monorepo 为准 (0.1.2-alpha.1, 与运行中的 rc.2 仅有 40 余行差异, 文中会标注差异点).
**A**:
用户回答: **两个都要** — Claude Code 和 Codex CLI 都纳入对比. 对比章节将并列覆盖两者, 各自给出源码依据: Codex 用 openai/codex 公开 Rust 源码, Claude Code 用 anthropics/claude-code 仓库的 SDK 源码 + 官方文档 (主循环为闭源二进制, 需注明依据边界).

}

## 完成记录 {
- 2026-09-05 第二版 (按用户批评重做):
- 正文 index.md 重写为读者优先: 主线图开场 -> 三件事 -> turn/step/pre-step 层次 -> compaction 机制 -> 三家对照表 -> 参考来源; 大段源码文件+行号罗列移除, 仅保留必要路径.
- 图表: 4 张独立 archify 图删除, 合并为 1 张 archify workflow 主线图 dsh-loop-main.html (schema v2, showcase 9/9 通过): 横向 输入(用户提问) -> 循环体(pre-step/模型调用/工具执行/判定) -> 输出(最终回复), 判定->模型调用 回边为循环, 侧泳道为上下文压缩触发点.
- PPT dsh-agent-loop-ppt.html 重做为 5 页单一 Deck, 中心是那张主线图, 不再让读者在多个 iframe 图之间切来切去.
- 旧文件 dsh-turn-loop.html / dsh-compaction.html / dsh-turn-sequence.html / engines-compare.html 已删除 (无任何引用).
- 文本零表情; 标点过 format_cn_punct.py (0 diff).
- Claude Code 依据边界保留: 闭源, 仅 SDK hooks/官方遥测事件, 不臆测内部实现.
}
## PPT 重构记录 (第二版) {
- 用户原话批评: PPT 一塌糊涂, 大段文本堆砌没法看, 分层图看不懂, 还分好几个图让人切来切去, 有些内容错的该去掉.
- 第二版做法: 一张图讲完主线 (输入->循环体->输出), step 子循环以回边表示; PPT 只保留这 1 张图为视觉核心, 其余 3 页是文字机制/对照表/总结.
- 三引擎对照从不可验证的推断收敛为简洁表格: DSH/Codex 有源码, Claude Code 只写官方可观察 hooks 事件.
}
## 渲染排障记录 (2026-09-05) {
- 现象: 用户反馈「DSH Agent Loop 主线图找不到页面」, 文章内嵌图位置空白/小方块, 全屏与新标签打开显示 Docusaurus 404 (找不到页面).
- 根因: dsh-loop-main.html 创建晚于 dev server 启动; ppt-html-assets 插件 (CopyWebpackPlugin) 在启动时枚举一次 html 文件, 新建的 html 不会被复制进 dev 输出, 于是该路径落到 SPA 404. 顺带确认: 旧版 4 张图当时也有同样问题 (从未在 dev 可见).
- 修复: 重启 pnpm docusaurus start --port 3000 后文件被复制, dsh-loop-main / dsh-agent-loop-ppt (带不带 .html) 均 200 且内容完整 (707KB/8.6KB). 以后新增/删除侧车 html 后必须重启 dev server.
- 相关: 生产构建不受影响 (copy 模式在 build 阶段重新枚举), 已验证 /tmp/hxloli-build-check 含两份完整文件.
}
## 源码关系修正记录 (2026-09-05) {
- 用户批评: 流程图与源码没关系 ("跟源码有个鸟的关系").
- 定位: DSH monorepo 在本机 /home/hx/Loli/code/AI-Code/gpt-json-to-token/deepseek-harness (packages/core/agent-loop). 旧 4 图引用的 agent-loop/src/agent.ts 是 rc.2 之前结构; 现行版本在 packages/core/agent-loop/src/agent.ts (ReactLoopAgent), inbox 在 packages/core/agent/src/inbox.ts, compaction-basic 在 packages/compaction/compaction-basic/src/.
- 修正: 流程图重画, 节点全部改用真实标识: Inbox(send/followup/steer→next-turn/next-step) → preStep()(agent/pre-step 瀑布) → step(): llm.stream(assistant/chunk) → executeToolCalls(tool/call+tool/result, 结果回灌 inbox next-step) → turn/end(completed/max-tokens/error); 压缩旁路挂在 agent/pre-step 与 agent/request-error(CONTEXT_WINDOW_EXCEEDED) 两个事件上.
- 正文重写为源码追踪式: 0x00 真实事件链(turn/start→step/start→assistant/chunk→tool/call→tool/result→assistant/message→step/end→turn/end, 依据 loop.spec.ts 断言) + 0x01 节点→源码映射表(agent.ts L232 preStep/L253 turn/L339 step/L442 buildRequest, tool-calls.ts) + 0x02 两层 while 真相(kick 驱动 → turn() 外层多 step → step() 内层多请求) + 0x03 compaction 双钩子与选区算法(region.ts, 0.8/0.16, toolPairingBalancedBefore, <compacted-summary>) + 0x04 文件地图.
- 全部源码声明经本机 monorepo 逐行核对 (行号、方法名、事件名、默认值).
}
## 4 版候选与定稿 (2026-09-05) {
- 用户要求做出多个可见版本 (不接受纯文字描述). 基于同一份真实源码交付 4 版: A 横向主线 (Inbox→preStep→stream↔tools→turn/end, 压缩旁路) / B 事件时序图 (5 生命线纵向 trace) / C 双泳道两层 while / D 压缩一等待遇. 全部通过 archify 9 项校验并 deliver 成独立 html.
- 用户选定 A. 已删除 B/C/D 与对比页 (dsh-turn-sequence.html / dsh-two-loops.html / dsh-compaction-view.html / dsh-diagrams-compare.html), 正文移除对比链接, 仅保留 dsh-loop-main.html (A) 与 dsh-agent-loop-ppt.html. 重启 dev server 清除副本表后验证: 文章只嵌主线图, 无 404, 标点检查通过.
}