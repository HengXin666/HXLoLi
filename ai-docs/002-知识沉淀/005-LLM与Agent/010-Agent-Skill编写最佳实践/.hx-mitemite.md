## 0x00 14d28669 begin {
**Q**:
本文与 009 (SkillOpt) 已按你的意见拆开: 009 只留训练机制与可复现性, 本文专讲编写最佳实践. 现在需要确认一个**深度边界**问题.

本文目前覆盖: 上下文预算(三级 + 三个数字)、触发工程(为什么 skill 不触发)、内容分层判据(自由度 + 三类资源边界)、正文硬规则、命名、可证伪的验证(forward-testing)、可执行校验、完整工作流. 全文按"推翻常见错误认知"组织, 不解释 skill 是什么.

**推荐答案: 保持当前范围, 不加"从零写一个 skill"的完整示例.**

**理由:** 你明确要的是"高级教程/最佳实践"而非"skill 是什么", 本文现在每节都在纠正一个具体错误做法. 加完整示例会把篇幅拉向教学型. 而且真正需要"照着做"的部分已经以可执行形式落在 hx-make-skill 这个 skill 里了(它自带 114 行 SKILL.md 与校验器), 笔记负责讲清 why, skill 负责承载 how.

**如果你希望加示例:** 建议加"一个真实 skill 的逐层拆解"而**不是**从零写一个 —— 即解剖一个已有且结构良好的 skill, 说明它每一层为什么这样分. 这样仍是"最佳实践"视角, 而非入门教学.

另外两个可选项(如需请说明): (1) 是否补一节"渐进式披露的三种拆分模式"(场景并置 / 条件细节 / 大文件目录), 这是官方提到但本文未展开的; (2) 是否把校验器接进项目 CI.
**A**:
}
