
# 分布式系统大厂面试高频知识点图解

## 一、分布式知识体系总览

```mermaid
mindmap
  root((分布式系统))
    理论基础
      CAP定理
      BASE理论
      一致性模型
    分布式协调
      分布式锁
      分布式ID
      分布式事务
    服务治理
      服务注册发现
      负载均衡
      熔断降级限流
      链路追踪
    数据层
      分库分表
      读写分离
      数据一致性
    高可用
      主从复制
      集群
      故障转移
    分布式存储
      一致性哈希
      分片策略
```

---

## 二、CAP 定理

```mermaid
graph TD
    CAP[CAP定理<br/>分布式系统最多同时满足两个] --> C[一致性 Consistency<br/>所有节点数据相同]
    CAP --> A[可用性 Availability<br/>每个请求都能得到响应]
    CAP --> P[分区容错性 Partition Tolerance<br/>网络分区时系统仍可运行]

    CP[CP系统] --> CP1["ZooKeeper / etcd / HBase<br/>保证一致性，牺牲可用性"]
    CA[CA系统] --> CA1["单机数据库<br/>不考虑分区（实际不存在）"]
    AP[AP系统] --> AP1["Eureka / Cassandra / DynamoDB<br/>保证可用性，牺牲一致性"]

    Note["网络分区一定会发生<br/>所以P必须保证<br/>实际是在C和A之间权衡"]

```

### BASE 理论（对CAP的妥协）

```mermaid
graph TD
    BASE --> BA[基本可用<br/>Basically Available<br/>允许响应时间增加/降级]
    BASE --> S[软状态<br/>Soft State<br/>允许中间状态存在]
    BASE --> E[最终一致性<br/>Eventually Consistent<br/>数据最终达成一致]
```

---

## 三、一致性协议

### Raft 协议

```mermaid
stateDiagram-v2
    [*] --> Follower
    Follower --> Candidate: 选举超时<br/>未收到心跳
    Candidate --> Leader: 获得多数票
    Candidate --> Follower: 发现更高任期的Leader
    Candidate --> Candidate: 选举超时<br/>重新选举
    Leader --> Follower: 发现更高任期

    note right of Leader: 处理所有客户端请求<br/>日志复制到Follower<br/>发送心跳
    note right of Follower: 响应Leader请求<br/>转发客户端请求到Leader
```

### Raft 日志复制流程

```mermaid
sequenceDiagram
    participant C as 客户端
    participant L as Leader
    participant F1 as Follower 1
    participant F2 as Follower 2

    C->>L: 写请求
    L->>L: 追加日志条目(uncommitted)
    L->>F1: AppendEntries RPC
    L->>F2: AppendEntries RPC
    F1->>L: 确认
    F2->>L: 确认
    Note over L: 多数节点确认 → 提交日志
    L->>L: 应用到状态机
    L->>C: 返回成功
    L->>F1: 通知提交(下次心跳)
    L->>F2: 通知提交(下次心跳)
```

### Paxos vs Raft vs ZAB 对比

| 对比项 | Paxos | Raft | ZAB |
|--------|-------|------|-----|
| 可理解性 | 难 | 容易 | 中等 |
| Leader | 不强制 | 强Leader | 强Leader |
| 日志 | 允许空洞 | 连续日志 | 连续日志 |
| 应用 | Google Chubby | etcd/Consul | ZooKeeper |

---

## 四、分布式事务

```mermaid
graph TD
    A[分布式事务方案] --> B[2PC 两阶段提交]
    A --> C[3PC 三阶段提交]
    A --> D[TCC Try-Confirm-Cancel]
    A --> E[本地消息表]
    A --> F[可靠消息最终一致]
    A --> G[Saga]
    A --> H[Seata框架]
```

### 2PC 两阶段提交

```mermaid
sequenceDiagram
    participant TM as 事务管理器<br/>协调者
    participant RM1 as 参与者1
    participant RM2 as 参与者2

    rect rgb(200, 235, 255)
    Note over TM, RM2: 阶段一：准备 Prepare
    TM->>RM1: 准备请求 (执行SQL但不提交)
    TM->>RM2: 准备请求
    RM1->>TM: OK / YES
    RM2->>TM: OK / YES
    end

    rect rgb(200, 255, 200)
    Note over TM, RM2: 阶段二：提交 Commit
    TM->>RM1: 提交
    TM->>RM2: 提交
    RM1->>TM: ACK
    RM2->>TM: ACK
    end

    Note over TM: 如果任意参与者准备失败<br/>→ 全部回滚
```

**2PC 缺点：** 同步阻塞、单点故障(协调者)、数据不一致风险

### TCC 补偿事务

```mermaid
graph TD
    subgraph Try 尝试阶段
        T1[预留资源<br/>冻结库存/冻结余额]
    end

    subgraph Confirm 确认阶段
        C1[确认提交<br/>扣减冻结的库存/余额]
    end

    subgraph Cancel 取消阶段
        X1[释放预留资源<br/>解冻库存/余额]
    end

    T1 -->|全部Try成功| C1
    T1 -->|任一Try失败| X1
```

### Saga 模式

```mermaid
graph TD
    T1[事务1<br/>创建订单] --> T2[事务2<br/>扣减库存]
    T2 --> T3[事务3<br/>扣减余额]
    T3 --> T4[事务4<br/>发送通知]

    T3 -.->|失败回滚| C3[补偿3<br/>退还余额]
    C3 -.-> C2[补偿2<br/>恢复库存]
    C2 -.-> C1[补偿1<br/>取消订单]

```

---

## 五、分布式锁

```mermaid
graph TD
    A[分布式锁实现方案] --> B[Redis]
    A --> C[ZooKeeper]
    A --> D[MySQL]

    B --> B1["SET key val NX EX 30<br/>+ Lua脚本释放<br/>+ 看门狗续期(Redisson)"]
    C --> C1["临时有序节点<br/>+ Watch机制<br/>天然避免死锁"]
    D --> D1["SELECT FOR UPDATE<br/>或唯一键插入<br/>性能最差"]

```

### Redis 分布式锁流程

```mermaid
sequenceDiagram
    participant C as 客户端
    participant R as Redis

    C->>R: SET lock_key uuid NX EX 30
    R->>C: OK (加锁成功)

    Note over C: 执行业务逻辑...
    Note over C: Redisson看门狗<br/>每10s续期到30s

    C->>R: Lua脚本释放锁<br/>if GET lock_key == uuid then DEL
    R->>C: OK

    Note over C,R: 为什么用UUID?<br/>防止误删别人的锁
```

### Redlock 算法（Redis多节点）

```mermaid
graph TD
    C[客户端] --> R1[Redis节点1]
    C --> R2[Redis节点2]
    C --> R3[Redis节点3]
    C --> R4[Redis节点4]
    C --> R5[Redis节点5]

    Note1["依次向5个独立Redis实例加锁<br/>超过半数(≥3)成功 + 总耗时<有效时间<br/>→ 加锁成功"]

```

---

## 六、分布式 ID

```mermaid
graph TD
    A[分布式ID方案] --> B[UUID<br/>无序 太长 不适合做主键]
    A --> C[数据库自增<br/>简单 有瓶颈]
    A --> D[号段模式<br/>每次取一批ID 减少DB压力]
    A --> E[Redis INCR<br/>简单高效 需持久化]
    A --> F[雪花算法 Snowflake<br/>趋势递增 64位]

```

### 雪花算法结构（64位）

```
 0 - 00000000 00000000 00000000 00000000 00000000 0 - 00000 - 00000 - 00000000 0000
 │                  41位时间戳                         5位     5位      12位
 │              (可用约69年)                        数据中心  机器ID   序列号
 符号位                                              ID              (4096/ms)
```

| 方案 | 有序性 | 可用性 | 适用场景 |
|------|--------|--------|----------|
| UUID | ❌ 无序 | 高 | 不要求有序的场景 |
| 数据库自增 | ✅ 严格递增 | 低(单点) | 小规模系统 |
| 号段模式 | ✅ 趋势递增 | 高 | 美团Leaf |
| 雪花算法 | ✅ 趋势递增 | 高 | Twitter/百度UidGenerator |

---

## 七、服务治理

### 服务注册与发现

```mermaid
graph TD
    subgraph 注册中心
        RC[Nacos / Eureka / ZooKeeper / Consul]
    end

    S1[服务A 实例1] -->|注册| RC
    S2[服务A 实例2] -->|注册| RC
    S3[服务A 实例3] -->|注册| RC

    C[服务B 消费者] -->|发现: 获取服务A实例列表| RC
    C -->|负载均衡调用| S1
    C -->|负载均衡调用| S2

    RC -->|健康检查/心跳| S1
    RC -->|健康检查/心跳| S2
    RC -->|健康检查/心跳| S3
```

### 负载均衡算法

```mermaid
graph TD
    LB[负载均衡算法] --> A[轮询 Round Robin]
    LB --> B[加权轮询 Weighted RR]
    LB --> C[随机 Random]
    LB --> D[加权随机]
    LB --> E[最少连接 Least Connections]
    LB --> F[一致性哈希 Consistent Hash]
    LB --> G[IP Hash]

```

### 熔断、限流、降级

```mermaid
graph TD
    subgraph 熔断 Circuit Breaker
        CB1[关闭] -->|失败率超阈值| CB2[打开<br/>直接返回错误]
        CB2 -->|超时后| CB3[半开<br/>放少量请求试探]
        CB3 -->|成功| CB1
        CB3 -->|失败| CB2
    end

    subgraph 限流 Rate Limiting
        RL1[计数器<br/>固定窗口]
        RL2[滑动窗口]
        RL3[漏桶<br/>恒定速率]
        RL4[令牌桶<br/>允许突发]
    end

    subgraph 降级 Degradation
        DG1[返回默认值/缓存数据]
        DG2[关闭非核心功能]
        DG3[简化处理流程]
    end
```

### 令牌桶 vs 漏桶

```mermaid
graph TD
    subgraph 漏桶 Leaky Bucket
        LB1[请求进入桶] --> LB2[恒定速率流出]
        LB3[桶满则丢弃]
        LB4["特点: 平滑流量<br/>不允许突发"]
    end

    subgraph 令牌桶 Token Bucket
        TB1[恒定速率放入令牌] --> TB2[请求取令牌才能通过]
        TB3[桶满令牌溢出丢弃]
        TB4["特点: 允许一定突发<br/>更灵活 Guava RateLimiter"]
    end
```

---

## 八、一致性哈希

### 传统取模 vs 一致性哈希

```mermaid
graph TD
    subgraph 传统取模 hash mod N
        TM1[节点增减时] --> TM2[大量Key重新映射]
        TM2 --> TM3[缓存大面积失效!]
    end

    subgraph 一致性哈希
        CH1["将节点映射到0~2^32的环上"]
        CH2[Key顺时针找到最近的节点]
        CH3[节点增减只影响相邻区间]
        CH4[虚拟节点解决数据倾斜]
    end

```

### 一致性哈希环

```
        Node A (hash=0)
           ╱          ╲
     Key1 ●            ● Key4
        ╱                ╲
   Node D                Node B
  (hash=270)           (hash=90)
        ╲                ╱
     Key3 ●            ● Key2
           ╲          ╱
        Node C (hash=180)

Key顺时针找到的第一个节点即为负责节点
增加/删除节点只影响环上相邻的一段
虚拟节点: 每个物理节点映射多个虚拟节点到环上，解决数据倾斜
```

---

## 九、分库分表

```mermaid
mindmap
  root((分库分表))
    垂直拆分
      垂直分库 按业务拆
        用户库 订单库 商品库
      垂直分表 按字段拆
        大字段/不常用字段拆出
    水平拆分
      水平分库
        相同结构 不同数据
      水平分表
        单表数据量过大时拆分
    分片策略
      Range范围
      Hash取模
      一致性哈希
    带来的问题
      分布式事务
      跨库JOIN
      排序分页
      分布式ID
      数据迁移扩容
    中间件
      ShardingSphere
      MyCat
      Vitess
```

### 分库分表路由

```mermaid
graph TD
    A[SQL请求] --> B[分片路由]
    B --> C{分片键}
    C -->|user_id % 4 = 0| D[DB0.user_0]
    C -->|user_id % 4 = 1| E[DB0.user_1]
    C -->|user_id % 4 = 2| F[DB1.user_2]
    C -->|user_id % 4 = 3| G[DB1.user_3]
```

---

## 十、微服务架构全景

```mermaid
graph TD
    Client[客户端] --> GW[API网关<br/>Kong/Nginx/Spring Cloud Gateway]
    GW --> Auth[认证鉴权<br/>JWT/OAuth2]
    GW --> RL[限流熔断<br/>Sentinel/Hystrix]

    GW --> S1[用户服务]
    GW --> S2[订单服务]
    GW --> S3[商品服务]
    GW --> S4[支付服务]

    S1 --> RPC[RPC调用<br/>Dubbo/gRPC/Feign]
    S2 --> RPC
    S3 --> RPC
    S4 --> RPC

    S2 --> MQ[消息队列<br/>Kafka/RocketMQ]
    MQ --> S4

    subgraph 基础设施
        REG[注册中心<br/>Nacos/Consul]
        CONF[配置中心<br/>Nacos/Apollo]
        TRACE[链路追踪<br/>Jaeger/Zipkin/SkyWalking]
        LOG[日志中心<br/>ELK]
        MON[监控告警<br/>Prometheus+Grafana]
    end

    S1 -.-> REG
    S2 -.-> REG
    S3 -.-> CONF
    S4 -.-> TRACE

```

---

## 十一、高频面试题速查

| 问题 | 答案要点 |
|------|----------|
| CAP定理是什么？ | 一致性+可用性+分区容错，最多同时满足两个；P必须保证，实际是C和A的权衡 |
| 什么是最终一致性？ | 允许数据暂时不一致，但经过一段时间后最终达成一致(BASE理论) |
| 分布式事务有哪些方案？ | 2PC/TCC/本地消息表/可靠消息/Saga；根据一致性要求选择 |
| 分布式锁怎么实现？ | Redis(SET NX+Lua+看门狗)/ZooKeeper(临时有序节点)/数据库(排他锁) |
| 如何保证接口幂等？ | Token机制/唯一ID+DB唯一键/Redis SetNX/状态机 |
| 什么是脑裂？ | 网络分区导致集群出现两个Leader，引发数据不一致 |
| 如何解决脑裂？ | 过半选举机制/fencing token/共享存储心跳 |
| 雪花算法时钟回拨怎么办？ | 等待/使用历史最大ID/备用机器ID |
| 什么是服务雪崩？ | 一个服务不可用导致调用链上游服务连锁故障 |
| 如何防止服务雪崩？ | 熔断(Hystrix/Sentinel)+限流+降级+超时控制+隔离 |
| 分库分表后如何排序分页？ | 每个分片查N条→内存归并排序→取前N条(深翻页性能差) |
| 微服务和SOA的区别？ | 微服务更细粒度、独立部署、去中心化；SOA偏ESB中心化 |
