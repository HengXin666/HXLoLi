
# 数据库大厂面试高频知识点图解

## 一、数据库整体知识体系

```mermaid
mindmap
  root((数据库))
    MySQL
      存储引擎
      索引
      事务
      锁
      日志
      SQL优化
    Redis
      数据结构
      持久化
      过期删除与淘汰
      缓存问题
      集群方案
    分库分表
      垂直拆分
      水平拆分
      分布式ID
```

---

## 二、MySQL 存储引擎

```mermaid
graph TB
    subgraph InnoDB 默认引擎
        I1[支持事务 ACID]
        I2[支持行级锁]
        I3[支持外键]
        I4[聚簇索引]
        I5[MVCC多版本并发控制]
        I6[支持崩溃恢复 redo log]
    end

    subgraph MyISAM
        M1[不支持事务]
        M2[表级锁]
        M3[不支持外键]
        M4[非聚簇索引]
        M5[支持全文索引]
        M6[读性能高 适合读多写少]
    end
```

---

## 三、MySQL 索引

### B+树结构

```mermaid
graph TD
    R["根节点<br/>10 ⏐ 20 ⏐ 30"] --> N1["叶子<br/>1,3,5,8"]
    R --> N2["叶子<br/>10,12,15,18"]
    R --> N3["叶子<br/>20,22,25,28"]
    R --> N4["叶子<br/>30,35,40,45"]

    N1 -->|双向链表| N2
    N2 -->|双向链表| N3
    N3 -->|双向链表| N4

```

### B+树 vs B树 vs Hash索引

| 对比项 | B+树 | B树 | Hash |
|--------|------|-----|------|
| 数据存储 | 只在叶子节点 | 所有节点 | 哈希桶 |
| 范围查询 | ✅ 叶子链表顺序扫描 | ❌ 需要中序遍历 | ❌ 不支持 |
| 等值查询 | O(log n) | O(log n) | O(1) |
| 排序 | ✅ 天然有序 | ❌ | ❌ |
| 磁盘IO | 少（非叶子不存数据，扇出大） | 多 | 不确定 |

### 聚簇索引 vs 非聚簇索引

```mermaid
graph TD
    subgraph 聚簇索引 主键索引
        CI[B+树叶子节点] --> CD[存储完整行数据]
    end

    subgraph 非聚簇索引 二级索引
        NI[B+树叶子节点] --> NK[存储主键值]
        NK -->|回表查询| CI
    end

    subgraph 覆盖索引
        COV[查询字段都在索引中] --> NOCB[无需回表!]
    end

```

### 索引失效场景

```mermaid
graph TD
    ROOT((索引失效)) --> A[最左前缀原则违反]
    ROOT --> B[对索引列做运算或函数]
    ROOT --> C[隐式类型转换]
    ROOT --> D[LIKE以%开头]
    ROOT --> E[OR条件部分无索引]
    ROOT --> F[NOT IN 或 NOT EXISTS]
    ROOT --> G[数据量太小]

    A --> A1["联合索引abc 只查bc"]
    B --> B1["WHERE YEAR❨create_time❩ = 2024"]
    C --> C1["字符串列不加引号"]
    D --> D1["LIKE '%abc'"]
    E --> E1["一侧有索引一侧没有"]
    F --> F1["某些情况优化器放弃索引"]
    G --> G1["优化器选择全表扫描"]

```

---

## 四、MySQL 事务

### ACID 特性

```mermaid
graph TD
    ACID[事务ACID] --> A[原子性 Atomicity<br/>undo log回滚]
    ACID --> C[一致性 Consistency<br/>最终目标]
    ACID --> I[隔离性 Isolation<br/>MVCC + 锁]
    ACID --> D[持久性 Durability<br/>redo log]

```

### 隔离级别

```mermaid
graph TD
    RU[读未提交<br/>Read Uncommitted] --> RC[读已提交<br/>Read Committed]
    RC --> RR[可重复读<br/>Repeatable Read]
    RR --> S[串行化<br/>Serializable]

    RU -.- P1[❌ 脏读]
    RC -.- P2[❌ 不可重复读]
    RR -.- P3["❌ 幻读(InnoDB用间隙锁解决)"]
    S -.- P4[✅ 全部解决<br/>但性能最差]
```

| 隔离级别 | 脏读 | 不可重复读 | 幻读 | 实现方式 |
|----------|------|-----------|------|----------|
| 读未提交 | ✅ | ✅ | ✅ | 无控制 |
| 读已提交 RC | ❌ | ✅ | ✅ | 每次读生成新 ReadView |
| **可重复读 RR(默认)** | ❌ | ❌ | ✅(InnoDB间隙锁解决) | 事务开始时生成 ReadView |
| 串行化 | ❌ | ❌ | ❌ | 加锁串行执行 |

### MVCC 原理

```mermaid
graph TD
    A[MVCC 多版本并发控制] --> B[每行数据的隐藏列]
    B --> B1[trx_id: 最近修改的事务ID]
    B --> B2[roll_pointer: 指向undo log旧版本]

    A --> C[ReadView 读视图]
    C --> C1[m_ids: 活跃事务ID列表]
    C --> C2[min_trx_id: 最小活跃事务ID]
    C --> C3[max_trx_id: 下一个将分配的事务ID]
    C --> C4[creator_trx_id: 创建者事务ID]

    A --> D[版本链]
    D --> D1[沿roll_pointer找到可见的版本]
```

**可见性判断规则：**
1. `trx_id == creator_trx_id` → 自己修改的，可见
2. `trx_id < min_trx_id` → 事务已提交，可见
3. `trx_id >= max_trx_id` → 事务在ReadView创建后开启，不可见
4. `min_trx_id <= trx_id < max_trx_id` → 看是否在 m_ids 中，在则不可见，不在则可见

---

## 五、MySQL 锁

```mermaid
mindmap
  root((MySQL锁))
    按粒度
      表级锁
        表锁
        元数据锁 MDL
        意向锁
      行级锁
        Record Lock 记录锁
        Gap Lock 间隙锁
        Next-Key Lock 临键锁
    按模式
      共享锁 S锁 读锁
      排他锁 X锁 写锁
    按思想
      乐观锁 版本号/CAS
      悲观锁 SELECT FOR UPDATE
```

### Next-Key Lock = Record Lock + Gap Lock

```mermaid
graph TD
    A["id: 1, 3, 5, 8"] --> B["Gap Lock: (-∞,1) (1,3) (3,5) (5,8) (8,+∞)"]
    A --> C["Record Lock: {1} {3} {5} {8}"]
    B --> D["Next-Key Lock: (-∞,1] (1,3] (3,5] (5,8] (8,+∞)"]

```

---

## 六、MySQL 日志

```mermaid
graph TD
    subgraph redo log 重做日志
        R1[InnoDB引擎层]
        R2[物理日志 记录页的修改]
        R3[保证持久性 崩溃恢复]
        R4[WAL机制 先写日志再写磁盘]
        R5[循环写 固定大小]
    end

    subgraph undo log 回滚日志
        U1[InnoDB引擎层]
        U2[逻辑日志 记录反向操作]
        U3[保证原子性 事务回滚]
        U4[MVCC版本链的基础]
    end

    subgraph binlog 归档日志
        B1[Server层]
        B2[逻辑日志 记录SQL语句]
        B3[主从复制的基础]
        B4[追加写 不会覆盖]
        B5["格式: Statement/Row/Mixed"]
    end
```

### 一条 UPDATE 的日志写入流程

```mermaid
sequenceDiagram
    participant E as 执行器
    participant I as InnoDB
    participant RL as redo log
    participant BL as binlog

    E->>I: UPDATE id=1 SET name='xx'
    I->>I: 读取id=1的数据页到Buffer Pool
    I->>I: 写undo log(旧值)
    I->>I: 更新Buffer Pool中的数据
    I->>RL: 写redo log (prepare状态)
    I->>E: 返回
    E->>BL: 写binlog
    E->>RL: 提交redo log (commit状态)
    Note over RL,BL: 两阶段提交<br/>保证redo log和binlog一致性
```

---

## 七、SQL 优化

```mermaid
mindmap
  root((SQL优化))
    EXPLAIN分析
      type: ALL→index→range→ref→eq_ref→const
      key: 实际使用的索引
      rows: 扫描行数
      Extra: Using index/filesort/temporary
    索引优化
      合理建立联合索引
      覆盖索引避免回表
      避免索引失效
    查询优化
      避免SELECT *
      小表驱动大表
      JOIN代替子查询
      LIMIT优化深翻页
    表设计优化
      合适的数据类型
      适度冗余
      垂直拆分大字段
    架构优化
      读写分离
      分库分表
      缓存层
```

### 深翻页优化

```sql
-- ❌ 慢：OFFSET越大越慢，需扫描 1000000+10 行
SELECT * FROM orders ORDER BY id LIMIT 1000000, 10;

-- ✅ 快：延迟关联 / 游标分页
SELECT * FROM orders WHERE id > 1000000 ORDER BY id LIMIT 10;

-- ✅ 快：子查询先走索引
SELECT * FROM orders 
INNER JOIN (SELECT id FROM orders ORDER BY id LIMIT 1000000, 10) AS t 
ON orders.id = t.id;
```

---

## 八、Redis 数据结构

```mermaid
mindmap
  root((Redis数据结构))
    String 字符串
      SDS动态字符串
      缓存/计数器/分布式锁
    List 列表
      QuickList 双向链表+压缩列表
      消息队列/时间线
    Hash 哈希
      Hashtable/Ziplist
      对象属性存储
    Set 集合
      Hashtable/Intset
      去重/交集并集差集
    ZSet 有序集合
      SkipList跳表 + Hashtable
      排行榜/延迟队列
    Bitmap 位图
      签到/布隆过滤器
    HyperLogLog
      基数统计 UV统计
    Stream
      消息队列 类似Kafka
```

### 跳表（ZSet底层）

```
Level 4:  1 ---------> 21 ----------------------------> NULL
Level 3:  1 ---------> 21 ---------> 41 --------------> NULL
Level 2:  1 ----> 11 -> 21 ----> 31 -> 41 ----> 51 ---> NULL
Level 1:  1 -> 6 -> 11 -> 21 -> 26 -> 31 -> 41 -> 46 -> 51 -> NULL
```

查询时间复杂度：O(log n)，空间换时间

---

## 九、Redis 持久化

```mermaid
graph TD
    subgraph RDB 快照
        R1[某一时刻全量数据快照]
        R2[fork子进程+COW写时复制]
        R3[恢复速度快]
        R4[可能丢失最后一次快照后的数据]
        R5["触发: save/bgsave/配置自动"]
    end

    subgraph AOF 追加日志
        A1[记录每条写命令]
        A2[数据完整性高]
        A3[文件体积大 恢复慢]
        A4["同步策略: always/everysec/no"]
        A5[AOF重写压缩文件]
    end

    subgraph 混合持久化 Redis4.0+
        M1[AOF重写时用RDB格式写入]
        M2[兼顾恢复速度和数据安全]
    end
```

---

## 十、Redis 缓存问题

```mermaid
graph TD
    subgraph 缓存穿透
        CP1[查询不存在的数据]
        CP2[缓存和DB都没有]
        CP3["解决: 布隆过滤器 / 缓存空值"]
    end

    subgraph 缓存击穿
        CB1[热点Key突然过期]
        CB2[大量请求直接打到DB]
        CB3["解决: 互斥锁 / 永不过期+异步更新"]
    end

    subgraph 缓存雪崩
        CA1[大量Key同时过期]
        CA2[或Redis宕机]
        CA3["解决: 过期时间加随机值 / 集群 / 熔断限流"]
    end
```

### 缓存与数据库一致性

```mermaid
graph TD
    A[写操作] --> B{Cache Aside 旁路缓存策略}
    B --> C[先更新数据库]
    C --> D[再删除缓存]
    D --> E[下次读取时回填缓存]

    F[延迟双删] --> G[删除缓存]
    G --> H[更新数据库]
    H --> I[延迟一段时间]
    I --> J[再次删除缓存]

```

---

## 十一、Redis 集群方案

```mermaid
graph TD
    A[Redis集群方案] --> B[主从复制]
    A --> C[哨兵 Sentinel]
    A --> D[Cluster 集群]

    B --> B1[读写分离<br/>从节点异步复制主节点]
    C --> C1[监控+自动故障转移<br/>主节点挂了自动选新主]
    D --> D1[16384个hash槽分片<br/>去中心化<br/>支持水平扩展]
```

---

## 十二、高频面试题速查

| 问题 | 答案要点 |
|------|----------|
| MySQL主键为什么推荐自增？ | 自增ID顺序插入，B+树不会频繁分裂；UUID随机插入导致页分裂 |
| count(*)和count(1)哪个快？ | InnoDB中两者性能基本相同，优化器会选最小的索引树扫描 |
| VARCHAR(100)和VARCHAR(10)区别？ | 磁盘存储一样(变长)，但内存分配和排序时VARCHAR(100)占用更多 |
| Redis为什么这么快？ | 内存操作 + 单线程避免锁竞争 + IO多路复用 + 高效数据结构 |
| Redis单线程为什么还能高并发？ | 瓶颈在网络IO不在CPU，epoll多路复用处理并发连接(6.0后IO多线程) |
| 如何实现分布式锁？ | Redis: SET key val NX EX + Lua释放; Redlock; Zookeeper临时有序节点 |
| 大Key问题怎么解决？ | 拆分(Hash分段) + 异步删除(UNLINK) + 定期扫描 |
| 热Key问题怎么解决？ | 本地缓存 + 读写分离 + Key打散(key_1, key_2) |
