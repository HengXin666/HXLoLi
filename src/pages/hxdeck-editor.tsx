import React from 'react';
import { ThemeEditor } from '../hxdeck';

/** 主题编辑器 —— 改配色 / 传图 / 调位置 / 导出 JSON|YAML */
const HxDeckEditorPage: React.FC = () => <ThemeEditor />;

export default HxDeckEditorPage;
